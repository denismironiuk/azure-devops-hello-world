trigger:
  - main

pool:
  name: Default   # self-hosted агент

variables:
  buildConfiguration: 'Release'
  buildVersion: '1.0.$(Build.BuildId)'
  artifactName: 'drop_$(Build.BuildNumber)'

steps:
# 1️⃣ Клонирование репозитория
- checkout: self

# 2️⃣ Установка .NET SDK
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '9.0.x'

# 3️⃣ Подготовка анализа SonarQube
- task: SonarQubePrepare@5
  inputs:
    SonarQube: 'SonarQubeConnection'   # имя service connection
    scannerMode: 'MSBuild'
    projectKey: 'HelloWorld'
    projectName: 'HelloWorld'
    projectVersion: '$(buildVersion)'
    extraProperties: |
  sonar.projectBaseDir=$(Build.SourcesDirectory)
  sonar.sourceEncoding=UTF-8
  sonar.verbose=true
  sonar.branch.analysis.mode=single

  displayName: '🔍 Prepare SonarQube Analysis'

# 4️⃣ Восстановление зависимостей
- script: |
    echo "🔧 Restoring dependencies..."
    dotnet restore ./HelloWorld/HelloWorld.csproj
  displayName: "Restore dependencies"

# 5️⃣ Сборка проекта с включённым анализом
- script: |
    echo "⚙️ Building project (with SonarQube)..."
    dotnet build ./HelloWorld/HelloWorld.csproj --configuration $(buildConfiguration)
  displayName: "Build project"

# 6️⃣ Запуск анализа SonarQube
- task: SonarQubeAnalyze@5
  displayName: '📊 Run SonarQube Analysis'

# 7️⃣ Публикация результатов анализа
- task: SonarQubePublish@5
  inputs:
    pollingTimeoutSec: '300'
  displayName: '📢 Publish SonarQube Quality Gate Result'

# 8️⃣ Архивация результата сборки
- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(Build.ArtifactStagingDirectory)'
    includeRootFolder: false
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/$(artifactName).zip'
    replaceExistingArchive: true
  displayName: "📦 Create versioned artifact archive"

# 9️⃣ Публикация артефактов в Azure DevOps
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)/$(artifactName).zip'
    ArtifactName: '$(artifactName)'
    publishLocation: 'Container'
  displayName: "🚀 Upload versioned artifact to Azure DevOps"
