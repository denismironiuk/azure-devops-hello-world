trigger:
- main

pool:
  name: Default

steps:
- checkout: self

- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '9.0.x'

- script: |
    echo "🔧 Restoring dependencies..."
    dotnet restore ./azure-devops-practice/HelloWorld/HelloWorld.csproj
  displayName: "Restore dependencies"

- script: |
    echo "⚙️ Building project..."
    dotnet build ./azure-devops-practice/HelloWorld/HelloWorld.csproj --configuration Release
  displayName: "Build project"

- script: |
    echo "🚀 Running project..."
    dotnet run --project ./azure-devops-practice/HelloWorld/HelloWorld.csproj
  displayName: "Run project"
